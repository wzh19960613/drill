> [学科] 来源 | 章节 @ 定位 > 题型

此处写题目，每个文件一个题目。

格式要求：

- 使用 Markdown 语法
- 关于图片：
    - 使用`![[图片路径]]`方式引入图片，图片路径可以为：
        - 文件名（推荐，与 Obsidian 兼容）
        - 相对路径
        - 绝对路径
        - URL
    - 图片类型可以为`svg`、`png`、`jpg`、`jpeg`、`webp`
    - 建议使用 SVG 矢量图，并建议使用如下方式对暗色模式进行适配：
    ```
    <style>
        @media (prefers-color-scheme: dark) {
            // 在暗色模式下的样式
        }
    </style>
    ```
- 公式使用`$公式$`或`$$公式$$`
- 关于选择题的选项：
  - 使用`(A) 选项内容`、`(B) 选项内容`的形式
  - 每行一个选项
  - 选项的上一行、下一行都应为空行，避免 Markdown 把内容渲染到同一行中
- 选择题默认为单选题，若为多选题请在题型写明`多选题`
- 如果在题目、答案或解析等内容中需要使用标题，则最高应为三级标题

## 答案

此处写答案，格式同上，`答案`与`解析`至少要存在一个。

## 解析

此处写解析，格式同上，`解析`与`答案`至少要存在一个。

## 备注

此处写备注，格式同上，可有可无。

关于文件的首行元数据的说明：

- `[学科] `：学科名称，如`数学`、`物理`、`化学`等，注意方括回后有一空格
- `来源`：题目来源，比如书名、网址、`2026 上海高考`等
- `| 章节`：题目所在的章节
- `@ 定位`：如何在`来源`中定位题目，比如`P25-12`、`03:15`、`第三题第二小问`等
- `> 题型`：`选择题`、`多选题`、`填空题`、`解答题`等
- 上述五个信息都是可选的，任何一项都可以不写，要注意符号格式
- 如果没有任何元数据，也可以不写首行，直接写题目内容