> [数学] 示例题库 · 演示 | 第2章 导数与应用 @ E5 > 证明题

证明：当 $x>0$ 时，$\ln(1+x)<x$。

## 解析

令 $f(t)=\ln(1+t)-t$，则 $f(0)=0$，且

$$f'(t)=\dfrac{1}{1+t}-1=-\dfrac{t}{1+t}$$

当 $t>0$ 时 $f'(t)<0$，$f$ 在 $[0,+\infty)$ 上单调递减，故 $f(x)<f(0)=0$，即 $\ln(1+x)<x$。

## 备注

示例：证明题可以只有 `## 解析`、没有 `## 答案`。
