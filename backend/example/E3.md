> 示例题库 · 演示 | 第1章 函数与极限 @ E3

$\lim\limits_{x\to 0}\dfrac{\sin 3x}{x}=$______。

## 答案

$3$。

## 解析

当 $x\to 0$ 时 $\sin 3x\sim 3x$，故原式 $=\lim\limits_{x\to 0}\dfrac{3x}{x}=3$。

## 备注

示例：省略 `[学科]` 与 `> 题型` 的写法——题型由题干中的 `______` 自动推断为填空题。
